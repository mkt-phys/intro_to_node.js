'use strict';

Object.defineProperty(exports, '__esModule', {
  value: true
});
exports.parseEngine = parseEngine;

var _browserParser = require('./browser-parser');

function parseEngine(userAgent) {
  if ((0, _browserParser.isFirefox)(userAgent)) {
    var gecko = userAgent.match(/rv:([\d\.]+).*\sgecko/i);
    var engineInfo = {
      name: 'Gecko'
    };

    if (gecko) {
      engineInfo.version = gecko[1];
    }

    return engineInfo;
  }

  return {};
}