'use strict';

Object.defineProperty(exports, '__esModule', {
  value: true
});
exports.isFirefox = isFirefox;
exports.firefoxInfo = firefoxInfo;
function parseFirefox(userAgent) {
  var firefox = userAgent.match(/\bfirefox\/([\d\.]+)/i);
  if (!firefox) {
    return {};
  }

  var version = firefox[1];
  return {
    name: 'Firefox',
    version: version,
    major: parseInt(version.split('.')[0], 10)
  };
}

function isFirefox(userAgent) {
  var result = parseFirefox(userAgent);
  return !!result.name;
}

function firefoxInfo(userAgent) {
  return parseFirefox(userAgent);
}