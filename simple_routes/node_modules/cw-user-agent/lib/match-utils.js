'use strict';

Object.defineProperty(exports, '__esModule', {
  value: true
});
exports.match = match;
exports.isApple = isApple;
exports.isAndroid = isAndroid;
exports.isWindows = isWindows;

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { 'default': obj }; }

var _regexp = require('./regexp');

var _regexp2 = _interopRequireDefault(_regexp);

/**
 * @param {RegExp} regex
 * @param {string} userAgent
 * @returns {boolean}
 */

function match(regex, userAgent) {
  return regex.test(userAgent);
}

/**
 * @param {string} userAgent
 * @returns {boolean}
 */

function isApple(userAgent) {
  return match(_regexp2['default'].apple.mac, userAgent) || match(_regexp2['default'].apple.phone, userAgent) || match(_regexp2['default'].apple.tablet, userAgent) || match(_regexp2['default'].apple.iPod, userAgent);
}

/**
 * @param {string} userAgent
 * @returns {boolean}
 */

function isAndroid(userAgent) {
  return match(_regexp2['default'].android.phone, userAgent) || match(_regexp2['default'].android.tablet, userAgent);
}

/**
 * @param {string} userAgent
 * @returns {boolean}
 */

function isWindows(userAgent) {
  return match(_regexp2['default'].windows.pc, userAgent);
}